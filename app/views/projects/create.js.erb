$(".new_project").html("<%= raw(escape_javascript(render('shared/project_form').html_safe))  %>");
$(".stats").html("<%= raw(escape_javascript(render('shared/stats').html_safe))  %>");
$(".projectsN").html("<%= raw(escape_javascript(render('shared/feedprojects').html_safe)) %>");


/*
$(document).querySelector('dataset.id').click(function() {//Нужна инициализация клика для новосозданного проекта, убрать удаляет проект после обновленмя страницы
    var delete_project = document.querySelector('.delete_project');
    $.ajax({
    url: '/projects/' + delete_project.dataset.id,
    type: 'DELETE',
    success: function(result) {
        $("#project-"+ delete_project.dataset.id).remove("#project-"+ delete_project.dataset.id);
    }
});
});*/