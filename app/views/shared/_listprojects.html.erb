<% if @projects.any? %>
  <ol class="projects items-projects-list">
    <!--  #project-
        render @list_itemsprojects
        render @projects
    -->
    <%= render @list_itemsprojects %>
  </ol>
<% end %>  
