<%= form_for(@project, remote: true) do |f| %><!--, remote: true aсинхроннй запрос -->
  
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :name, placeholder: "+ add Project" %>
  </div>
  <%= f.submit "add Project", class: "btn btn-primary" %>

<% end %>