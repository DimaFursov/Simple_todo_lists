<% provide(:title, @user.name) %>
<p>
  <strong><i>Name:</i>:</strong>
  <%= current_user.name %>
</p>
<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<div class="projectsN">
    <h3>Projects (<%= @user.projects.count %>)</h3>
    <ol class="projects">
      <%= render @projects %>
    </ol>    
</div>